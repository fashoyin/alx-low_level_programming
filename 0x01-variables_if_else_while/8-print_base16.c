#include <stdio.h>
/**
 * main -main
 *
 * Description: Alx
 *
 * Return: Always true when 0
 */

int main(void)
{
	int i;

	for (i = 0; i <= 10; i++)
		putchar(i);
	for (j = 'a'; j <= f; j++)
		putchar(j);
	putchar('\n');
	return (0);
}
