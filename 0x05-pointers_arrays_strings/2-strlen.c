#include "main.h"
/**
 * _strlen - Gets the length of a string
 * @s: The string being considered
 * Return: Return length of string
 */

int _strlen(char *s)
{
	int a;

	int c = 0;
	for (a = 0; s[a] != '\0'; a++)
	{
		c++
	}
	return (0);
}
